/*
 * HorizontalRule Messages
 *
 * This contains all the text for the HorizontalRule component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.HorizontalRule.header',
    defaultMessage: 'This is the HorizontalRule component !',
  },
});
